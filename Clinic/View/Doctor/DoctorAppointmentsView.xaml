<UserControl x:Class="Clinic.View.Doctor.DoctorAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <TextBlock Text="Прийоми"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

        <DataGrid ItemsSource="{Binding Appointments}"
                  AutoGenerateColumns="False"
                  Margin="0,40,0,0"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Пацієнт" Binding="{Binding PatientName}" Width="*" />
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="120" />
                <DataGridTextColumn Header="Час" Binding="{Binding Time}" Width="80" />
                <DataGridTextColumn Header="Причина" Binding="{Binding Reason}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
